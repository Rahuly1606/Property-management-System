# Server Configuration
spring.main.allow-bean-definition-overriding=true

# Database Configuration - Using environment variables from Dockerfile
spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# JWT Configuration
app.jwt.secret=secret123456789
app.jwt.expiration=86400000

# File Upload Configuration
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Cloudinary Configuration
cloudinary.cloud-name=dt8xx5yyr
cloudinary.api-key=338782571496263
cloudinary.api-secret=QRaGSP5Ki02u4WV_nWrTe79QTzE

# Razorpay Configuration
razorpay.key.id=rzp_test_pDJ1iM9OmQY99d
razorpay.key.secret=aTlEyT9XBo0vcUttzpUb8RN3

# Logging Configuration
logging.level.org.springframework.security=INFO
logging.level.org.springframework.web=INFO
logging.level.com.propertymanagement=INFO

# Email Configuration (for notifications)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=alexrahul9576@gmail.com
spring.mail.password=njqo npol ttof tick
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# CORS Configuration - Allow frontend container
spring.web.cors.allowed-origins=http://frontend,http://localhost
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=*
spring.web.cors.allow-credentials=true